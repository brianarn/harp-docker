{
  "name": "harp-docker",
  "version": "0.0.1",
  "description": "A simple containerized version of a Harp.js web app, using Docker",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prestart": "if [ ! -d site ]; then harp init site; fi",
    "start": "harp server site"
  },
  "author": "Brian Sinclair",
  "license": "MIT",
  "dependencies": {
    "harp": "^0.20.3"
  }
}
